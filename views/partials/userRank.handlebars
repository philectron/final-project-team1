<div class="rankContainer">
  <h1>{{username.name}}</h1>
  <div class="graph-container">
    <h2 class="graph-label">Your progress to becoming gym rat {{username.name}}</h2>
    {{#each username.totalProgress}}
      {{>goal}}
    {{/each}}
    {{#each username.goals}}
      {{>goal}}
    {{/each}}
  </div>
