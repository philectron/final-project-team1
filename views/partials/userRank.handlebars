<div class="rankContainer">
  <h1 class="rankName">{{name}}</h1>
  <div class="graph-container">
    <h2 class="graph-label">{{name}}'s weekly stats</h2>
    {{#each totalProgress}}
      {{>goal}}
    {{/each}}
    {{#each goals}}
      {{>goal}}
    {{/each}}
  </div>
